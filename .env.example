# Application
PORT=3000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=valuation
DB_SSL=false

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@localhost:5672

# Redis (for caching)
REDIS_HOST=localhost
REDIS_PORT=6379

# External APIs (for initial sync)
VECTOR_API_URL=http://localhost:3001
VECTOR_API_TOKEN=
AGGREGATOR_API_URL=http://localhost:3002
AGGREGATOR_API_TOKEN=

# Initial Sync Configuration
SYNC_ON_STARTUP=false
SYNC_BATCH_SIZE=100
# Set to true to skip initial sync (useful for local development)
SKIP_INITIAL_SYNC=true

# External services (for production)
# DB_SSL=true
# DB_HOST=your-production-db-host
